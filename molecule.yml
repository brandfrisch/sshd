---
dependency:
  name: galaxy

docker:
  containers:
    - name: sshd
      image: brandfrisch/ubuntu-16.04
      image_version: 'latest'
      command: /lib/systemd/systemd
      privileged: true
      port_bindings: {
        22: 2222
      }

verifier:
  name: goss